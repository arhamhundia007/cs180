public class HardWork {
    /*int x = currentPassagePointer;
                    if (passage.charAt(currentPassagePointer) == word.charAt(0)){
                        String varCheck = passage.substring(currentPassagePointer, currentPassagePointer + wordSize);
                        varCheck = varCheck.toLowerCase();

                        if (word.equals(varCheck) && (passage.charAt(currentPassagePointer+wordSize) == '.' || passage.charAt(currentPassagePointer+wordSize) == '?' || passage.charAt(currentPassagePointer+wordSize) == ' ' || passage.charAt(currentPassagePointer+wordSize) == '!' || passage.charAt(currentPassagePointer+wordSize) == ',')){
                            passage = passage.replace(word, replace);
                        }
                    }*/
                    /*int x = currentPassagePointer;
                    int counter = 0; //keeps count of how many times each does the answer come true

                    for (int j = 0; j < wordSize; j++) {
                        if (passage.charAt(x) == word.charAt(j)) {
                            counter = counter + 1;
                            x = x + 1;
                        } else if (!(counter == wordSize)){
                            counter = 0;
                            break;
                        } else {
                            break;
                        }// end if

                    }//end loop

                    if ((counter == wordSize) && ((passage.charAt(currentPassagePointer) + (wordSize-1)) == '' || (passage.charAt(currentPassagePointer-1) == ' '))) {
                        passage = passage.replace(word, replace);
                        int remainingPassageSize;
                        if (currentPassagePointer <= wordSize){
                            remainingPassageSize = wordSize;
                        } else  {
                            remainingPassageSize = currentPassagePointer - wordSize;
                        }




                    }// end if*/

}
